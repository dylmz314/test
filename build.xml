<project name="TestProject" default="install" basedir=".">
    <!-- Clean hedefi -->
    <target name="clean">
        <echo>Cleaning the project...</echo>
    </target>

    <!-- Install hedefi -->
    <target name="install" depends="clean">
        <echo>Installing the project...</echo>
    </target>
</project>